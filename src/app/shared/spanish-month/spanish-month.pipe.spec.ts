import { SpanishMonthPipe } from './spanish-month.pipe';

describe('SpanishMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new SpanishMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
